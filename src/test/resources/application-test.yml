server:
  port: 0

spring:
  data:
    elasticsearch:
      cluster-name: testCluster
      cluster-nodes: localhost:9400
      host: localhost
      port: 9400
      repositories:
        enabled: true
      properties:
        http:
          enabled: true

xpack:
  security:
    enabled: false

search:
  queryLimitInHours: 24

file:
  data:
    directory: ./testdata/